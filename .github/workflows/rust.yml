name: Rust CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

env:
  CARGO_TERM_COLOR: always
  SODIUM_INCLUDE_DIR: /usr/include
  SODIUM_LIB_DIR: /usr/lib/x86_64-linux-gnu

jobs:
  setup:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Install Rust
      uses: dtolnay/rust-toolchain@stable
      with:
        toolchain: stable

    - name: Install libsodium with headers
      run: |
        sudo apt-get update
        sudo apt-get install -y libsodium-dev build-essential pkg-config
        echo "Checking sodium.h location..."
        find /usr -name "sodium.h" 2>/dev/null || echo "sodium.h not found in /usr"
        find /usr/local -name "sodium.h" 2>/dev/null || echo "sodium.h not found in /usr/local"

    - name: Cache Rust dependencies
      uses: actions/cache@v4
      with:
        path: |
          ~/.cargo/registry
          ~/.cargo/git
          target
        key: ${{ runner.os }}-cargo-${{ hashFiles('**/Cargo.lock') }}
        restore-keys: |
          ${{ runner.os }}-cargo-

  build:
    runs-on: ubuntu-latest
    needs: setup
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Build Rust project with C FFI
      env:
        PKG_CONFIG_PATH: /usr/lib/x86_64-linux-gnu/pkgconfig
      run: |
        echo "Building with C FFI support..."
        cargo build --release --verbose
      
    - name: Build verification
      run: |
        if [ -f ./target/release/passlock ]; then
          echo "Rust binary compiled: ./target/release/passlock"
          file ./target/release/passlock
        else
          echo "Rust binary not found"
          exit 1
        fi

  rust-tests:
    runs-on: ubuntu-latest
    needs: setup
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Run Rust tests
      env:
        PKG_CONFIG_PATH: /usr/lib/x86_64-linux-gnu/pkgconfig
      run: cargo test --verbose

  clippy:
    runs-on: ubuntu-latest
    needs: setup
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Install Clippy
      uses: dtolnay/rust-toolchain@stable
      with:
        toolchain: stable
        components: clippy

    - name: Run Clippy
      env:
        PKG_CONFIG_PATH: /usr/lib/x86_64-linux-gnu/pkgconfig
      run: cargo clippy -- -D warnings

  formatting:
    runs-on: ubuntu-latest
    needs: setup
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Install rustfmt
      uses: dtolnay/rust-toolchain@stable
      with:
        toolchain: stable
        components: rustfmt

    - name: Check formatting
      run: cargo fmt -- --check

  verification:
    runs-on: ubuntu-latest
    needs: [build, rust-tests, clippy, formatting]
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Final verification
      run: |
        echo "Final Rust verification:"
        
        if ./target/release/passlock --help 2>&1 | head -2; then
          echo "Rust CLI works correctly"
        elif ./target/release/passlock -h 2>&1 | head -2; then
          echo "Rust CLI works correctly"
        else
          timeout 3 ./target/release/passlock 2>&1 | head -2 && echo "TUI starts (no --help flag)" || echo "TUI may have issues"
        fi
        
        echo "Rust CI completed successfully!"